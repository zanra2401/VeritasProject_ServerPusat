PORT=5001
# Note: API_KEY validation is now per-lembaga. Each lembaga has its own unique API key stored in the database.
# API keys are auto-generated during seeding and can be regenerated via /lembaga/:id/regenerate-key endpoint.
DB_USER=postgres
DB_PASSWORD=
DB_NAME=server_pusat
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DIALECT=postgres
